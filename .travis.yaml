language: python
python:
  - 2.7
  - 3.4
  - 3.5
  - "nightly"
matrix:
  allow_failures:
    - python: 3.5
    - python: "nightly"
sudo: false
notifications:
  email:
    vaggarwa@redhat.com
script:
  - "python -c 'import yaml,sys;yaml.safe_load(sys.stdin)' < instance.yaml"

